```### DEVOPS LEARNING ###

    Вопросы которые я искал в интернете

1. Мониторинг

    1. mkdir -p "Создаёт все недостающие директории в пути, а не только последнюю"
    2. Scrapes — периодически опрашивает твои endpoints (обычно каждые 15-30 сек)
       Stores — сохраняет метрики в свою БД
       Serves — отдаёт данные через API (для Grafana)
    3. source "Читает и выполняет команды из файла в текущем процессе shell, а не в дочернем."
    4. docker compose -f "Флаг -f (или --file) позволяет указать какой именно docker-compose.yml файл использовать."
    5. depends_on: в Docker Compose используется для управления порядком запуска сервисов
    6. 

``` 