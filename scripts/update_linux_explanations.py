#!/usr/bin/env python3
"""
Обновление описаний к вопросам по Linux.
Делает объяснения более подробными и запоминающимися.
"""

import sys
import os

# Добавляем backend в path
project_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
backend_dir = os.path.join(project_dir, 'backend')
sys.path.insert(0, backend_dir)
os.chdir(backend_dir)

from sqlalchemy import create_engine, text
from sqlalchemy.orm import sessionmaker
from app.core.config import settings

# Создаем движок и сессию для PostgreSQL
engine = create_engine(settings.DATABASE_URL)
SessionLocal = sessionmaker(bind=engine)
db = SessionLocal()

# Новые подробные описания для вопросов
explanations = {
    # Intern уровень
    448: """📂 **Команда `cd` (Change Directory)** — одна из самых базовых команд для навигации в файловой системе Linux.

**Что происходит:**
- При выполнении `cd /home/user` вы переходите из текущей директории в `/home/user`
- Без аргументов `cd` возвращает в домашнюю директорию пользователя (`~`)
- `cd ..` поднимает на уровень выше (в родительскую директорию)
- `cd -` возвращает в предыдущую директорию

**💡 Запомните:** `cd` не перемещает файлы — она перемещает **вас** (вашу текущую рабочую позицию) по файловой системе.""",

    449: """🔊 **Команда `echo`** — простой, но мощный инструмент для вывода текста.

**Что происходит:**
- `echo "Hello World"` выводит строку в терминал (стандартный вывод)
- Текст в кавычках передаётся как аргумент команде
- Можно использовать для вывода переменных: `echo $HOME`
- С перенаправлением `>` записывает в файл: `echo "text" > file.txt`

**💡 Запомните:** `echo` — это «голос» терминала. Используйте его в скриптах для вывода сообщений пользователю или отладки.""",

    450: """📋 **Команда `ls` (list)** — ваш основной инструмент для просмотра содержимого директорий.

**Популярные опции:**
- `ls` — простой список файлов
- `ls -l` — подробный список (права, владелец, размер, дата)
- `ls -a` — показывает скрытые файлы (начинающиеся с `.`)
- `ls -lh` — размеры в человекочитаемом формате (KB, MB, GB)
- `ls -la` — комбинация: подробно + скрытые файлы

**💡 Запомните:** В Linux файлы, начинающиеся с `.`, считаются скрытыми. `ls` без опций их не показывает!""",

    451: """👆 **Команда `touch`** — создание пустых файлов или обновление временных меток.

**Что происходит:**
- Если файл **не существует** — создаётся пустой файл
- Если файл **существует** — обновляется время его последнего изменения (mtime)
- Не изменяет содержимое файла!

**Примеры использования:**
- `touch file.txt` — создать пустой файл
- `touch *.py` — обновить метки всех Python-файлов
- В скриптах: создание файлов-флагов для标记 выполнения задач

**💡 Запомните:** `touch` = «коснуться». Файл остаётся прежним, но система «помнит», что вы его коснулись.""",

    452: """🆔 **Команда `whoami`** — быстрый способ узнать, от чьего имени вы работаете.

**Что происходит:**
- Выводит имя текущего пользователя
- Эквивалентно `id -un`
- Полезно при работе с `sudo` — чтобы проверить, не стали ли вы root

**Связанные команды:**
- `id` — подробная информация (UID, GID, группы)
- `who` — все пользователи, вошедшие в систему
- `users` — список имён вошедших пользователей

**💡 Запомните:** Если вы работаете с сервером и не уверены в своих правах — первым делом `whoami`!""",

    453: """📁 **Команда `mkdir` (make directory)** — создание новых папок.

**Что происходит:**
- `mkdir new_folder` создаёт директорию с именем `new_folder`
- Создавать можно только в директории, где у вас есть права на запись

**Полезные опции:**
- `mkdir -p dir1/dir2/dir3` — создаёт цепочку вложенных директорий
- `mkdir folder1 folder2` — создание нескольких директорий сразу

**💡 Запомните:** Без опции `-p` команда ошибётся, если родительская директория не существует. `-p` = «parents» = «создай родителей, если нет».""",

    454: """🗑️ **Команда `rm` (remove)** — удаление файлов. **Осторожно!**

**Что происходит:**
- `rm file.txt` удаляет файл безвозвратно (не в корзину!)
- В Linux нет «корзины» по умолчанию — удалённые файлы трудно восстановить

**Опасные опции:**
- `rm -r folder/` — рекурсивное удаление директории со всем содержимым
- `rm -f file.txt` — принудительное удаление без подтверждения
- `rm -rf folder/` — **комбинация силы и рекурсии: удаляет всё без вопросов**

**💡 Запомните:** `rm -rf /` — самая опасная команда в Linux (удалит всю систему). Всегда проверяйте путь перед удалением!""",

    455: """📍 **Команда `pwd` (Print Working Directory)** — показывает, где вы находитесь.

**Что происходит:**
- Выводит полный (абсолютный) путь от корня `/` до текущей директории
- Например: `/home/user/projects/myapp`

**Когда использовать:**
- После серии `cd` — чтобы понять, где вы оказались
- В скриптах — для сохранения текущего положения
- При работе с относительными путями — для ориентации

**💡 Запомните:** `pwd` — это ваш GPS в файловой системе. Заблудились? Спросите: «Где я?» → `pwd`""",

    456: """📄 **Команда `cat` (concatenate)** — вывод содержимого файлов.

**Что происходит:**
- `cat file.txt` выводит всё содержимое файла в терминал
- Может объединять несколько файлов: `cat file1 file2`
- С перенаправлением создаёт файлы: `cat > new.txt`

**Для больших файлов лучше:**
- `head file.txt` — первые 10 строк
- `tail file.txt` — последние 10 строк
- `less file.txt` — постраничный просмотр с прокруткой

**💡 Запомните:** `cat` хорош для коротких файлов. Для логов и больших файлов используйте `less` или `tail -f`.""",

    457: """📋 **Команда `cp` (copy)** — копирование файлов и директорий.

**Что происходит:**
- `cp file1.txt file2.txt` создаёт копию `file1.txt` с именем `file2.txt`
- Если `file2.txt` существует — он будет **перезаписан без предупреждения!**

**Полезные опции:**
- `cp -r folder/ backup/` — копирование директории рекурсивно
- `cp -i file1 file2` — запрос подтверждения перед перезаписью
- `cp -v file1 file2` — показывает, что копируется (verbose)

**💡 Запомните:** `cp` не перемещает, а **дублирует**. Оригинал остаётся на месте.""",

    458: """🔄 **Команда `ps` (process status)** — список запущенных процессов.

**Что происходит:**
- `ps` без опций показывает процессы текущего пользователя в текущем терминале
- `ps aux` — **все процессы** в системе (самый популярный вариант)
- `ps aux | grep nginx` — найти процессы nginx

**Колонки вывода:**
- USER — владелец процесса
- PID — идентификатор процесса
- %CPU, %MEM — использование ресурсов
- COMMAND — команда запуска

**💡 Запомните:** `ps aux` — ваш первый шаг при поиске «что тут запущено?» или «какой PID у процесса?».""",

    459: """✏️ **Команда `mv` (move)** — перемещение **и** переименование файлов.

**Что происходит:**
- `mv old.txt new.txt` — переименование (в той же директории)
- `mv file.txt /home/user/` — перемещение в другую папку
- `mv file1 file2 /backup/` — перемещение нескольких файлов

**Важно:**
- Файл не копируется — он **перемещается** (исчезает со старого места)
- При перемещении на другой диск фактически происходит копирование + удаление

**💡 Запомните:** В Linux нет отдельной команды «переименовать». `mv` = «переместить», а переименование — это перемещение внутри той же папки.""",

    460: """⚠️ **Команда `rm -r`** — рекурсивное удаление директорий.

**Что происходит:**
- `rm -r folder/` удаляет директорию и **всё её содержимое**
- Удаляет вложенные папки, файлы, подпапки — всё до основания
- Безвозвратно! Восстановление сложно или невозможно

**Опасные комбинации:**
- `rm -rf folder/` — `-f` (force) отменяет все подтверждения
- `rm -rf /` — **удалит всю систему!** (не запускайте!)

**💡 Запомните:** Перед `rm -r` трижды проверьте путь. Ошибка здесь стоит очень дорого.""",

    461: """📖 **Команда `man` (manual)** — встроенная документация Linux.

**Что происходит:**
- `man ls` открывает руководство по команде `ls`
- Содержит: описание, все опции, примеры, файлы, авторов
- Навигация: `↑/↓` — прокрутка, `/поиск` — найти, `q` — выход

**Структура man-страницы:**
1. NAME — название и краткое описание
2. SYNOPSIS — синтаксис команды
3. DESCRIPTION — подробное описание
4. OPTIONS — все опции с пояснениями
5. EXAMPLES — примеры использования

**💡 Запомните:** `man` — ваш лучший учитель. Прежде чем гуглить — проверьте `man <команда>`!""",

    462: """📄 **Команда `head`** — просмотр начала файла.

**Что происходит:**
- `head file.txt` выводит первые 10 строк
- `head -n 20 file.txt` — первые 20 строк
- `head -c 100 file.txt` — первые 100 байт

**Когда использовать:**
- Быстро посмотреть структуру файла
- Проверить заголовки CSV/JSON
- Убедиться, что файл не пустой

**💡 Запомните:** `head` + `tail` = быстрый просмотр файла с двух концов. `head` для начала, `tail` для конца (и логов).""",

    463: """🔐 **Команда `chmod 755`** — установка прав доступа.

**Что означают цифры:**
- `7` (владелец) = `rwx` = чтение + запись + выполнение
- `5` (группа) = `r-x` = чтение + выполнение
- `5` (остальные) = `r-x` = чтение + выполнение

**Расшифровка:**
- `r` (read) = 4, `w` (write) = 2, `x` (execute) = 1
- Складываем: 7 = 4+2+1, 5 = 4+1

**Когда использовать:**
- `chmod 755 script.sh` — сделать скрипт исполняемым для всех
- `chmod 644 file.txt` — файл доступен для чтения всеми, запись только владельцу

**💡 Запомните:** `755` = «все могут читать и выполнять, владелец может всё». Стандарт для скриптов.""",

    464: """💾 **Команда `df -h` (disk free)** — свободное место на дисках.

**Что показывает:**
- Размер каждого раздела
- Использовано и доступно места
- Точку монтирования

**Опция `-h`:**
- Выводит размеры в человекочитаемом формате (GB, MB)
- Без `-h` — в байтах (неудобно)

**Когда использовать:**
- «Почему сервер тормозит?» → проверьте `df -h` (возможно, диск полон)
- Перед копированием больших файлов
- Для мониторинга заполненности `/var/log`

**💡 Запомните:** `df` = «disk free». Если сервер «упал» — первым делом `df -h`!""",

    465: """📜 **Команда `history`** — история ваших команд.

**Что показывает:**
- Список всех ранее введённых команд с номерами
- Хранится в `~/.bash_history` (для bash)
- `history | grep docker` — найти команды с docker

**Полезные трюки:**
- `!123` — выполнить команду под номером 123
- `!!` — повторить последнюю команду
- `!docker` — выполнить последнюю команду, начинающуюся с «docker»
- `Ctrl+R` — интерактивный поиск по истории

**💡 Запомните:** Вы не помните команду, которую вводили 5 минут назад? `history | grep <что-то>` спасёт вас!""",

    466: """🔗 **Pipe `|` (конвейер)** — передача вывода одной команды на вход другой.

**Как работает:**
- `ls -l | grep txt` — вывод `ls` передаётся в `grep`, который фильтрует строки с «txt»
- `cat file.txt | head -n 5` — показать первые 5 строк файла
- `ps aux | grep nginx | wc -l` — посчитать количество процессов nginx

**Философия Unix:**
- Каждая команда делает **одно дело хорошо**
- Конвейеры позволяют комбинировать простые инструменты для сложных задач

**💡 Запомните:** `|` = «передай дальше». Вывод слева → вход справа. Цепочка команд = конвейер обработки.""",

    467: """🔍 **Команда `find`** — поиск файлов по имени и другим критериям.

**Синтаксис:**
- `find . -name "*.txt"` — найти все `.txt` файлы в текущей директории и подпапках
- `find /home -name "config.yml"` — найти конкретный файл
- `find . -type f -size +100M` — файлы больше 100 МБ

**Полезные опции:**
- `-type f` — только файлы, `-type d` — только директории
- `-mtime -7` — изменённые за последние 7 дней
- `-exec rm {} \;` — выполнить команду для каждого найденного файла

**💡 Запомните:** `find` ищет **по критериям** (имя, размер, дата). `grep` ищет **по содержимому**. Это разные инструменты!""",

    # Junior уровень
    468: """🔎 **Команда `grep -r`** — рекурсивный поиск текста в файлах.

**Что происходит:**
- `grep -r "error" /var/log/` ищет строку «error» во **всех файлах** `/var/log/` и подпапках
- `-r` (recursive) — спускается во вложенные директории
- Выводит: `путь/к/файлу:строка_с_найдённым`

**Полезные опции:**
- `grep -i "error"` — игнорировать регистр (Error, ERROR, error)
- `grep -v "debug"` — вывести строки **без** «debug» (invert)
- `grep -l "text"` — показать только имена файлов (без строк)
- `grep -n "text"` — показать номера строк

**💡 Запомните:** `grep` = «поиск по содержимому». Логи, конфиги, код — `grep` найдёт нужную строку за секунды.""",

    469: """🛑 **`kill` vs `kill -9`** — мягкое и жёсткое завершение процессов.

**`kill PID` (SIGTERM, сигнал 15):**
- **Вежливая просьба** завершиться
- Процесс может сохранить данные, закрыть соединения, освободить ресурсы
- Процесс **может проигнорировать** сигнал (редко)

**`kill -9 PID` (SIGKILL, сигнал 9):**
- **Мгновенное убийство** ядром системы
- Процесс **не может** проигнорировать или обработать
- **Не сохраняет данные!** Может повредить файлы

**Когда что использовать:**
1. Сначала `kill PID` — дайте процессу шанс завершиться красиво
2. Если не реагирует 5-10 секунд → `kill -9 PID`

**💡 Запомните:** `kill -9` — это «ядерная кнопка». Используйте только когда процесс не отвечает на обычный `kill`.""",

    470: """🔌 **Команда `netstat -tlnp` / `ss -tlnp`** — какие порты слушает сервер.

**Расшифровка опций:**
- `-t` — TCP порты
- `-l` — только слушающие (listening)
- `-n` — числовые адреса (не разрешать имена)
- `-p` — показать PID/имя программы

**Пример вывода:**
```
tcp   0   0   0.0.0.0:80   0.0.0.0:*   LISTEN   1234/nginx
```
→ nginx (PID 1234) слушает порт 80 на всех интерфейсах

**Современная замена:** `ss -tlnp` (быстрее, точнее)

**💡 Запомните:** «Какой сервис занимает порт 8080?» → `ss -tlnp | grep 8080`. Порт < 1024 требует root!""",

    471: """📇 **Inode (index node)** — «паспорт» файла в Linux.

**Что хранит inode:**
- ✅ Права доступа (chmod)
- ✅ Владелец и группа (chown)
- ✅ Размер файла
- ✅ Временные метки (создание, изменение, доступ)
- ✅ Указатели на блоки данных на диске

**Что НЕ хранит inode:**
- ❌ Имя файла (хранится в директории)
- ❌ Содержимое файла (хранится в блоках данных)

**Важные факты:**
- У каждого файла ровно один inode
- Несколько имён (жёстких ссылок) могут указывать на один inode
- `ls -i` покажет номер inode

**💡 Запомните:** Inode = «досье файла». Имя файла — это просто «ярлык», привязанный к inode.""",

    472: """👥 **Команда `usermod -aG`** — добавление пользователя в группу.

**Расшифровка:**
- `-a` = append (добавить, не удаляя из других групп)
- `-G` = дополнительные группы (не основная)

**Пример:**
```bash
usermod -aG docker username
```
→ Добавить `username` в группу `docker` (для запуска Docker без sudo)

**Важно:**
- ⚠️ Без `-a` пользователь будет **удалён из всех других групп**!
- 🔄 После изменения нужно **перелогиниться** (или `newgrp docker`)

**Проверка:**
```bash
groups username  # Показать все группы пользователя
```

**💡 Запомните:** `usermod -aG` = «добавить в группу». Всегда используйте `-a`, иначе потеряете другие группы!""",

    473: """📊 **Команда `ps aux | wc -l`** — подсчёт процессов.

**Как работает:**
1. `ps aux` — выводит все процессы (по одному на строку)
2. `|` — передаёт вывод в следующую команду
3. `wc -l` (word count --lines) — считает строки

**Результат:**
- Примерно = количество процессов + 1 (заголовок)
- Точнее: `ps aux --no-headers | wc -l`

**Другие использования `wc`:**
- `wc -w file.txt` — количество слов
- `wc -c file.txt` — количество байт
- `wc -l file.txt` — количество строк

**💡 Запомните:** `wc -l` = «посчитай строки». Конвейер + `wc` = быстрый подсчёт чего угодно.""",

    474: """⏰ **Cron (crontab)** — планировщик задач.

**Формат записи в crontab:**
```
* * * * * команда
│ │ │ │ │
│ │ │ │ └─ день недели (0-7, 0 и 7 = воскресенье)
│ │ │ └─── месяц (1-12)
│ │ └───── день месяца (1-31)
│ └─────── час (0-23)
└───────── минута (0-59)
```

**Примеры:**
- `* * * * * /script.sh` — каждую минуту
- `0 * * * * /script.sh` — каждый час в 0 минут
- `0 2 * * * /script.sh` — каждый день в 02:00
- `0 0 * * 0 /script.sh` — каждое воскресенье в полночь

**Команды:**
- `crontab -e` — редактировать расписание
- `crontab -l` — показать текущее расписание

**💡 Запомните:** 5 звёзд = минута, час, день, месяц, день недели. `*` = «каждый».""",

    475: """🔗 **Hard link vs Soft link (символическая ссылка)** — два типа ссылок.

**Hard link (жёсткая ссылка):**
- Это **дополнительное имя** для того же inode
- Не занимает дополнительного места на диске
- Работает, даже если оригинал перемещён
- ❌ Нельзя создать для директории
- ❌ Нельзя создать на другом диске/разделе

**Soft link (символическая ссылка, symlink):**
- Это **отдельный файл**, содержащий путь к оригиналу
- Занимает немного места (хранит путь)
- Если оригинал удалён → становится «битой» ссылкой
- ✅ Можно создать для директории
- ✅ Можно создать на другом диске

**Команды:**
- `ln file link` — жёсткая ссылка
- `ln -s file link` — символическая ссылка

**💡 Запомните:** Hard link = «второе имя файла». Soft link = «ярлык» (как в Windows).""",

    476: """📦 **Команда `ldd`** — показать зависимости бинарного файла.

**Что показывает:**
- Какие разделяемые библиотеки (.so файлы) нужны программе
- Где они находятся в системе
- Все ли библиотеки найдены

**Пример:**
```bash
ldd /usr/bin/python3
```
Вывод:
```
libpython3.8.so.1.0 => /usr/lib/x86_64-linux-gnu/libpython3.8.so.1.0
libc.so.6 => /lib/x86_64-linux-gnu/libc.so.6
```

**Когда использовать:**
- Ошибка «library not found» при запуске программы
- Проверка, какие библиотеки использует программа
- Диагностика проблем с зависимостями

**💡 Запомните:** `ldd` = «list dynamic dependencies». Программа не запускается? `ldd` покажет, чего не хватает!""",

    477: """❌ **Ошибка «Permission denied»** — нет прав на выполнение.

**Причины:**
1. ⚠️ **Нет бита выполнения (x)** — самая частая причина
   ```bash
   chmod +x script.sh  # Добавить право на выполнение
   ```

2. ⚠️ **Нет прав на чтение файла**
   ```bash
   chmod u+r script.sh  # Дать владельцу право чтения
   ```

3. ⚠️ **Нет прав на выполнение в текущей директории**
   ```bash
   chmod +x .  # Дать право выполнения в директории
   ```

**Проверка прав:**
```bash
ls -l script.sh
# -rw-r--r-- 1 user user 1234 Jan 1 12:00 script.sh
#    ^^^ нет 'x'!
```

**💡 Запомните:** Скачали скрипт из интернета? Скорее всего, нужно `chmod +x` перед запуском!""",

    478: """🌍 **Переменные окружения** — настройки, доступные всем программам.

**Что это:**
- Именованные значения, влияющие на поведение программ
- Примеры: `PATH`, `HOME`, `USER`, `LANG`

**Как посмотреть:**
- `env` — все переменные окружения
- `printenv` — аналогично
- `printenv PATH` — конкретную переменную
- `echo $HOME` — вывести значение переменной

**Как установить:**
- `export VAR=value` — установить в текущей сессии
- Добавить в `~/.bashrc` — установить навсегда

**Важные переменные:**
- `PATH` — где искать исполняемые файлы
- `HOME` — домашняя директория
- `PWD` — текущая директория

**💡 Запомните:** Переменные окружения = «глобальные настройки системы». `$VAR` = «значение переменной».""",

    479: """📦 **Команда `tar -czf`** — создание архива.

**Расшифровка опций:**
- `-c` = create (создать архив)
- `-z` = gzip (сжать с помощью gzip)
- `-f` = file (имя файла архива)

**Примеры:**
```bash
tar -czf data.tar.gz data/     # Создать сжатый архив
tar -xzf data.tar.gz           # Распаковать архив
tar -tzf data.tar.gz           # Показать содержимое (без распаковки)
tar -czf archive.tar.gz file1 file2  # Архивировать несколько файлов
```

**Другие форматы:**
- `tar -cjf archive.tar.bz2` — сжатие bzip2 (медленнее, но сильнее)
- `tar -cJf archive.tar.xz` — сжатие xz (ещё сильнее)

**💡 Запомните:** `tar` = «лента» (tape archive). `-czf` = «создать, сжать gzip, в файл». `-xzf` = «извлечь, сжат gzip, из файла».""",

    480: """👑 **Команда `chown`** — смена владельца файла.

**Синтаксис:**
```bash
chown user:group file.txt
```

**Что делает:**
- Меняет владельца файла на `user`
- Меняет группу файла на `group`

**Примеры:**
- `chown root:root file.txt` — владелец и группа root
- `chown www-data:www-data /var/www` — типично для веб-серверов
- `chown -R user:group folder/` — рекурсивно для всей папки

**Важно:**
- ⚠️ Только root может менять владельца на другого пользователя
- 👤 Обычный пользователь может менять группу на одну из своих групп

**💡 Запомните:** `chown` = «change owner». Владелец файла решает, кому давать права (через `chmod`).""",

    481: """🔍 **Как узнать, какой процесс слушает порт 80?**

**Способ 1: `lsof`**
```bash
sudo lsof -i :80
```
Вывод:
```
COMMAND   PID   USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
nginx    1234   www-data  6u  IPv4  12345      0t0  TCP *:http (LISTEN)
```
→ nginx (PID 1234) слушает порт 80

**Способ 2: `ss`**
```bash
sudo ss -tlnp | grep :80
```

**Способ 3: `netstat`** (устаревший, но ещё встречается)
```bash
sudo netstat -tlnp | grep :80
```

**Почему sudo?**
- Порты < 1024 считаются «привилегированными»
- Только root может видеть информацию о них

**💡 Запомните:** «Кто занял порт 8080?» → `sudo lsof -i :8080` или `sudo ss -tlnp | grep 8080`.""",

    482: """🔑 **SSH ключи** — безопасная аутентификация без пароля.

**Что это:**
- Пара криптографических ключей: **приватный** и **публичный**
- Приватный ключ хранится у вас (никому не давать!)
- Публичный ключ размещается на сервере

**Как работает:**
1. Генерируете пару ключей: `ssh-keygen -t rsa -b 4096`
2. Копируете публичный ключ на сервер: `ssh-copy-id user@server`
3. Подключаетесь без пароля: `ssh user@server`

**Файлы:**
- `~/.ssh/id_rsa` — приватный ключ (chmod 600!)
- `~/.ssh/id_rsa.pub` — публичный ключ
- `~/.ssh/authorized_keys` — список разрешённых публичных ключей (на сервере)

**💡 Запомните:** Приватный ключ = пароль. Никогда не передавайте `.ssh/id_rsa` никому. Публичный ключ можно раздавать свободно.""",

    483: """🚫 **`grep -v`** — инвертированный поиск (исключить строки).

**Что делает:**
- `grep -v "debug" file.txt` — вывести все строки, **кроме** содержащих «debug»
- `-v` = invert (инвертировать, перевернуть)

**Примеры использования:**
```bash
ps aux | grep -v grep          # Исключить сам процесс grep из вывода
cat logfile | grep -v "INFO"   # Показать только предупреждения и ошибки
cat config | grep -v "^#"      # Показать конфиг без комментариев
```

**Комбинации:**
- `grep -v "debug" | grep -v "trace"` — исключить несколько паттернов
- `grep -v -E "debug|trace"` — то же самое с regex

**💡 Запомните:** `grep` = «найти», `grep -v` = «найти всё, кроме». Полезно для фильтрации «шума» в логах.""",

    484: """🔄 **nohup, screen, tmux** — запуск процессов после закрытия терминала.

**Проблема:**
- При закрытии терминала все запущенные процессы получают сигнал SIGHUP
- По умолчанию процесс завершается

**Решение 1: `nohup`**
```bash
nohup ./long_script.sh &
```
- Игнорирует SIGHUP
- Вывод перенаправляется в `nohup.out`
- ❌ Нельзя «вернуться» к процессу

**Решение 2: `screen`**
```bash
screen -S mysession    # Создать сессию
./long_script.sh       # Запустить процесс
Ctrl+A, D              # Отсоединиться
screen -r mysession    # Присоединиться позже
```

**Решение 3: `tmux`** (современная замена screen)
```bash
tmux new -s mysession  # Создать сессию
Ctrl+B, D              # Отсоединиться
tmux attach -t mysession  # Присоединиться
```

**💡 Запомните:** `nohup` = «забыл и ушёл». `screen/tmux` = «вернусь позже». Для долгих задач используйте `tmux`!""",

    485: """📡 **`tail -f`** — просмотр логов в реальном времени.

**Что делает:**
- `tail -f /var/log/syslog` — показывает последние строки и **продолжает следить** за новыми
- `-f` = follow (следовать)

**Полезные опции:**
- `tail -f -n 50 logfile` — показать последние 50 строк, затем следить
- `tail -F logfile` — как `-f`, но продолжает, если файл переименован (для ротации логов)

**Примеры:**
```bash
tail -f /var/log/nginx/access.log   # Следить за запросами к веб-серверу
tail -f -n 100 app.log              # Последние 100 строк + новые
journalctl -f                       # Аналог для systemd
```

**Остановка:** `Ctrl+C`

**💡 Запомните:** `tail -f` = «прямая трансляция» логов. Отладка в реальном времени!""",

    486: """📍 **`which`, `whereis`, `type`** — где находится команда?

**`which command`:**
- Ищет исполняемый файл в `$PATH`
- Выводит первый найденный путь
```bash
which python3  # /usr/bin/python3
```

**`whereis command`:**
- Ищет также исходники и man-страницы
```bash
whereis python3  # python3: /usr/bin/python3 /usr/share/man/man1/python3.1.gz
```

**`type command` (встроенная в bash):**
- Показывает, что такое команда
```bash
type cd    # cd is a shell builtin
type ls    # ls is aliased to `ls --color=auto`
type python3  # python3 is /usr/bin/python3
```

**💡 Запомните:** `which` = «где файл?». `type` = «что это такое?» (builtin, alias или файл).""",

    487: """🌐 **`ping`, `nc`** — проверка доступности хоста.

**`ping hostname`:**
- Отправляет ICMP echo-запросы
- Проверяет базовую сетевую связность
```bash
ping google.com
ping 8.8.8.8
```
- `Ctrl+C` для остановки

**`nc -zv hostname port` (netcat):**
- Проверяет доступность конкретного **порта**
- `-z` = zero-I/O mode (сканирование)
- `-v` = verbose (подробно)
```bash
nc -zv google.com 443    # Проверить HTTPS
nc -zv localhost 3306    # Проверить MySQL
```

**Другие инструменты:**
- `traceroute hostname` — показать маршрут пакетов
- `mtr hostname` — комбинированный ping + traceroute

**💡 Запомните:** `ping` = «ты там?» (хост). `nc` = «порт открыт?» (сервис).""",

    # Middle уровень
    488: """📂 **`lsof -p PID`** — какие файлы открыты у процесса.

**Что показывает:**
- Все открытые файловые дескрипторы процесса
- Обычные файлы, сокеты, пайпы, устройства

**Пример:**
```bash
lsof -p 1234
```
Вывод:
```
COMMAND  PID USER   FD   TYPE DEVICE SIZE/OFF  NODE NAME
python  1234 user  cwd    DIR  0,25     4096   123  /home/user/app
python  1234 user  txt    REG  0,25   123456   456  /usr/bin/python3
python  1234 user    3u  IPv4  12345      0t0  TCP *:8080 (LISTEN)
```

**Колонки:**
- FD = File Descriptor (cwd, txt, 0, 1, 2, 3u...)
- TYPE = тип (DIR, REG, IPv4...)
- NAME = путь или сокет

**Когда использовать:**
- Ошибка «too many open files» — найти утечку
- Узнать, какой файл читает/пишет процесс
- Найти процессы, использующие определённый файл

**💡 Запомните:** В Linux «всё есть файл». `lsof` покажет, какие «файлы» использует процесс (включая сокеты и пайпы).""",

    489: """🔐 **`sudo` vs `su`** — два способа стать root.

**`sudo command`:**
- Выполняет **одну команду** от имени root
- Требует пароль **текущего пользователя** (не root!)
- Пользователь должен быть в группе `sudo`
- Логирует все выполненные команды
```bash
sudo apt update  # Введите свой пароль
```

**`su` (switch user):**
- Запускает **новую оболочку** от имени другого пользователя
- Требует пароль **целевого пользователя** (root)
- `su -` — с загрузкой окружения целевого пользователя
```bash
su -   # Введите пароль root
# Теперь вы root, до выхода
exit
```

**`sudo -i`:**
- Как `su -`, но требует пароль текущего пользователя
- Виртуальный вход в root

**💡 Запомните:** `sudo` = «выполни команду как root». `su` = «стань другим пользователем». `sudo` безопаснее (логи, не нужно знать пароль root).""",

    490: """🔍 **`find /var -type f -size +100M`** — поиск больших файлов.

**Расшифровка:**
- `/var` — где искать
- `-type f` — только файлы (не директории)
- `-size +100M` — больше 100 мегабайт

**Другие примеры:**
```bash
find / -size +1G              # Файлы больше 1 ГБ
find /home -name "*.log"      # Все .log файлы
find . -mtime -7              # Изменённые за последние 7 дней
find . -type f -exec rm {} \; # Найти и удалить все файлы (осторожно!)
```

**Полезные опции:**
- `-mtime -N` — изменённые N дней назад
- `-name "pattern"` — по имени (wildcards `*` и `?`)
- `-exec command {} \;` — выполнить команду для каждого файла

**💡 Запомните:** `find` = «найди файлы по критериям». Диск полон? `find / -size +500M` покажет «пожирателей места»!""",

    491: """🚀 **GRUB** — загрузчик Linux.

**Что делает:**
- Загружает ядро Linux при старте системы
- Позволяет выбрать версию ядра (если их несколько)
- Показывает меню при загрузке

**`update-grub`:**
- Сканирует `/boot` на наличие ядер
- Генерирует `/boot/grub/grub.cfg`
- Обновляет меню загрузчика

**Когда запускать:**
- После установки нового ядра
- После изменения параметров ядра в `/etc/default/grub`
- Если нужно добавить/удалить ядро из меню

**Конфигурация:**
```bash
sudo nano /etc/default/grub  # Редактировать настройки
sudo update-grub             # Применить изменения
```

**💡 Запомните:** GRUB = «первая программа, которая запускается». `update-grub` = «обнови меню загрузки».""",

    492: """⚙️ **systemd** — менеджер инициализации и служб.

**Что делает:**
- Первая программа, запускаемая ядром (PID 1)
- Управляет сервисами (демонами)
- Параллельный запуск служб (быстрее старых init)

**Основные команды:**
```bash
systemctl start nginx        # Запустить сервис
systemctl stop nginx         # Остановить
systemctl restart nginx      # Перезапустить
systemctl status nginx       # Показать статус
systemctl enable nginx       # Добавить в автозагрузку
systemctl disable nginx      # Убрать из автозагрузки
```

**Unit-файлы:**
- `/etc/systemd/system/` — пользовательские сервисы
- `/lib/systemd/system/` — системные сервисы

**💡 Запомните:** `systemctl enable` = «добавить в автозагрузку». `start` ≠ `enable` (запуск ≠ автозагрузка).""",

    493: """🐌 **`systemd-analyze blame`** — почему система долго загружается?

**Что показывает:**
- Время загрузки каждого сервиса
- Сортировка по длительности (самые долгие сверху)

**Пример вывода:**
```
         15.234s NetworkManager-wait-online.service
         10.123s docker.service
          5.678s snapd.service
```

**`systemd-analyze critical-chain`:**
- Показывает «узкое место» — зависимость, которая задерживает загрузку
- Визуализирует цепочку зависимостей

**Как ускорить загрузку:**
1. `systemd-analyze blame` — найти медленные сервисы
2. `systemctl disable slow-service` — отключить ненужные
3. `systemctl mask slow-service` — полностью заблокировать

**💡 Запомните:** Долгая загрузка? `systemd-analyze blame` покажет виновника. Отключите ненужные сервисы!""",

    494: """🤝 **TCP three-way handshake** — установка соединения.

**Три шага:**
1. **SYN** → Клиент отправляет серверу: «Хочу соединиться!» (sequence number = X)
2. **SYN-ACK** ← Сервер отвечает: «ОК, я готов!» (acknowledge = X+1, свой sequence = Y)
3. **ACK** → Клиент подтверждает: «Договорились!» (acknowledge = Y+1)

**После этого:**
- Соединение установлено
- Начинается передача данных

**Зачем это нужно:**
- Синхронизация последовательностей (sequence numbers)
- Гарантия, что обе стороны готовы к передаче
- Проверка доступности сервера

**Завершение соединения (4 шага):**
- FIN → ACK ← FIN → ACK (каждая сторона закрывает свою сторону)

**💡 Запомните:** SYN → SYN-ACK → ACK = «привет, привет, ок». TCP надёжен, потому что устанавливает соединение перед передачей данных.""",

    495: """🛡️ **`iptables -A INPUT -s IP -j DROP`** — блокировка IP.

**Расшифровка:**
- `-A INPUT` — добавить правило в цепочку INPUT (входящие пакеты)
- `-s IP` — источник (source) = указанный IP
- `-j DROP` — действие (jump) = отбросить пакет без ответа

**Примеры:**
```bash
iptables -A INPUT -s 192.168.1.100 -j DROP     # Блокировать IP
iptables -A INPUT -p tcp --dport 22 -j DROP    # Блокировать SSH
iptables -A INPUT -p tcp --dport 80 -j ACCEPT  # Разрешить HTTP
```

**Просмотр правил:**
```bash
iptables -L -n -v  # Показать все правила
```

**Сохранение:**
- Правила сбрасываются после перезагрузки!
- `iptables-save > /etc/iptables/rules.v4` — сохранить

**💡 Запомните:** `iptables` = «стена между вами и интернетом». `-A INPUT -s IP -j DROP` = «этого не пускать».""",

    496: """📊 **top vs htop** — мониторы процессов.

**`top` (классический):**
- ✅ Встроен во все системы
- ❌ Неудобный интерфейс
- ❌ Нет прокрутки
- ❌ Нет цветов

**`htop` (современный):**
- ✅ Цветная индикация (CPU, память)
- ✅ Прокрутка по горизонтали и вертикали
- ✅ Управление мышью и F-клавишами
- ✅ Древовидный вид процессов (F5)
- ✅ Прямой kill процесса (F9)
- ❌ Нужно устанавливать отдельно

**Установка:**
```bash
apt install htop    # Debian/Ubuntu
yum install htop    # CentOS/RHEL
```

**💡 Запомните:** `top` = «есть везде». `htop` = «удобнее, но ставь сам». Поставьте `htop` — не пожалеете!""",

    497: """💾 **Восстановление удалённого файла через `/proc`**.

**Ситуация:**
- Файл удалён (`rm file.txt`)
- Но процесс ещё держит его открытым

**Решение:**
1. Найти PID процесса:
   ```bash
   lsof | grep deleted
   ```
   Вывод: `python  1234  user  3u  REG  0,25  1234  456  /tmp/file.txt (deleted)`

2. Скопировать из `/proc`:
   ```bash
   cp /proc/1234/fd/3 /tmp/recovered.txt
   ```

**Почему это работает:**
- В Linux файл считается удалённым, когда удалена **последняя ссылка на inode**
- Но если процесс держит файл открытым — данные остаются на диске
- `/proc/PID/fd/` — символические ссылки на открытые файлы процесса

**💡 Запомните:** Файл удалён, но процесс ещё работает? `/proc/PID/fd/номер` спасёт данные!""",

    498: """📏 **`/etc/security/limits.conf`** — лимиты ресурсов.

**Что ограничивает:**
- Максимальное число открытых файловых дескрипторов
- Максимальный размер файла (core dump)
- Максимальное количество процессов
- Приоритет (nice)

**Синтаксис:**
```
username  type  item  value
```

**Примеры:**
```
# Увеличить лимит открытых файлов для пользователя app
app  soft  nofile  65535
app  hard  nofile  65535

# Запретить пользователю user создавать core dump
user  hard  core  0
```

**Типы лимитов:**
- `soft` — мягкий (можно увеличить до hard)
- `hard` — жёсткий (максимум, может уменьшить только root)

**Проверка:**
```bash
ulimit -n  # Текущий лимит открытых файлов
```

**💡 Запомните:** Ошибка «too many open files»? Увеличьте `nofile` в `limits.conf`!""",

    499: """🎯 **Kernel space vs User space** — два мира Linux.

**Kernel space (пространство ядра):**
- 🔐 Привилегированный режим процессора
- 📦 Прямой доступ к оборудованию (диск, память, сеть)
- 🛡️ Изолировано от пользовательских программ
- 📊 Здесь работает ядро, драйверы, модули

**User space (пространство пользователя):**
- 👤 Обычный режим процессора
- 🚫 Нет прямого доступа к железу
- 📞 Взаимодействие с ядром через **системные вызовы** (syscalls)
- 📊 Здесь работают ваши программы

**Системные вызовы:**
- `open()`, `read()`, `write()`, `close()`
- `fork()`, `exec()`, `exit()`
- `socket()`, `bind()`, `connect()`

**💡 Запомните:** Ядро = «охранник с прямым доступом ко всему». Программы = «посетители, которые просят через системные вызовы». Это обеспечивает безопасность и стабильность!""",

    500: """🔐 **`openssl req -x509`** — самоподписанный SSL-сертификат.

**Команда:**
```bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
```

**Расшифровка опций:**
- `-x509` — формат сертификата (стандарт X.509)
- `-newkey rsa:4096` — сгенерировать новый RSA ключ 4096 бит
- `-keyout key.pem` — сохранить приватный ключ
- `-out cert.pem` — сохранить сертификат
- `-days 365` — действителен 365 дней
- `-nodes` — no DES, без парольной фразы на ключ

**Когда использовать:**
- ✅ Тестирование HTTPS локально
- ✅ Внутренние сервисы в закрытой сети
- ❌ Публичные сайты (нужен сертификат от CA)

**Проверка:**
```bash
openssl x509 -in cert.pem -text -noout  # Показать сертификат
```

**💡 Запомните:** Самоподписанный сертификат = «я сам себе доверяю». Браузеры будут ругаться, но для тестов подходит!""",

    501: """🔬 **`strace`** — трассировка системных вызовов.

**Что показывает:**
- Все системные вызовы процесса
- Сигналы, полученные процессом
- Аргументы и возвращаемые значения

**Примеры:**
```bash
strace ls                    # Трассировать команду ls
strace -p 1234               # Прицепиться к запущенному процессу
strace -o log.txt command    # Записать вывод в файл
strace -e open,read command  # Только вызовы open и read
```

**Когда использовать:**
- Программа падает без сообщения об ошибке
- «Где программа ищет конфиг?»
- «Почему нет доступа к файлу?»
- Отладка «немых» ошибок

**Пример вывода:**
```
open("/etc/myapp.conf", O_RDONLY) = -1 ENOENT (No such file or directory)
```
→ Программа ищет `/etc/myapp.conf`, но файл не найден!

**💡 Запомните:** `strace` = «рентген для программы». Видно, какие файлы она открывает, какие ошибки получает.""",

    502: """🌉 **Сетевой мост (bridge)** — виртуальный коммутатор.

**Что делает:**
- Объединяет несколько сетевых интерфейсов на L2 (канальный уровень)
- Работает как виртуальный свитч
- Пакеты передаются между интерфейсами

**Настройка:**
```bash
# Создать мост
ip link add br0 type bridge

# Добавить интерфейс в мост
ip link set eth0 master br0

# Поднять мост
ip link set br0 up
```

**Когда использовать:**
- 🖥️ Виртуальные машины (KVM, LXC) — чтобы VM имели доступ к физической сети
- 🔄 Объединение сетей
- 🧪 Тестирование сетевых конфигураций

**Проверка:**
```bash
bridge link show  # Показать интерфейсы в мосту
```

**💡 Запомните:** Bridge = «виртуальный свитч». VM подключаются к мосту → получают доступ к физической сети.""",

    503: """🛡️ **SELinux / AppArmor** — принудительный контроль доступа.

**Что делает:**
- Ограничивает возможности процессов **даже с root-правами**
- Политика определяет, что можно делать каждому процессу
- Блокирует несанкционированные действия

**SELinux (Red Hat, CentOS):**
- `getenforce` — текущий режим
- `setenforce 0` — временно отключить (Permissive mode)
- `setenforce 1` — включить (Enforcing mode)
- `/etc/selinux/config` — постоянная настройка

**AppArmor (Ubuntu, Debian):**
- `aa-status` — статус профилей
- `systemctl disable apparmor` — отключить

**Когда отключать:**
- ⚠️ Приложение не работает из-за политик безопасности
- ⚠️ Для тестирования (но не в production!)

**💡 Запомните:** SELinux/AppArmor = «дополнительный охранник для root». `setenforce 0` = «охранник смотрит, но не вмешивается».""",

    504: """🪞 **`mdadm --create`** — создание RAID 1 (зеркало).

**Команда:**
```bash
mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb1 /dev/sdc1
```

**Расшифровка:**
- `/dev/md0` — имя RAID-массива
- `--level=1` — RAID 1 (зеркалирование)
- `--raid-devices=2` — два диска
- `/dev/sdb1 /dev/sdc1` — разделы для зеркалирования

**Что делает RAID 1:**
- Данные дублируются на обоих дисках
- Если один диск выходит из строя — второй работает
- Ёмкость = размер одного диска (второй для резерва)

**Дальнейшие шаги:**
```bash
# Создать файловую систему
mkfs.ext4 /dev/md0

# Смонтировать
mount /dev/md0 /mnt/raid

# Сохранить конфигурацию
mdadm --detail --scan >> /etc/mdadm/mdadm.conf
```

**💡 Запомните:** RAID 1 = «зеркало». Два диска, данные одинаковые. Один умер — второй работает.""",

    505: """📤 **rsyslog** — отправка логов на удалённый сервер.

**Настройка клиента:**
```bash
sudo nano /etc/rsyslog.conf
```

**Добавить строку:**
```
*.* @remote-host:514    # UDP
*.* @@remote-host:514   # TCP (надёжнее)
```

**Расшифровка:**
- `*.*` — все facility и все severity (все логи)
- `@` — UDP (быстрее, но без гарантий)
- `@@` — TCP (надёжнее, с подтверждением)
- `514` — стандартный порт syslog

**На сервере:**
```bash
# Включить приём UDP в /etc/rsyslog.conf
module(load="imudp")
input(type="imudp" port="514")
```

**Перезапуск:**
```bash
systemctl restart rsyslog
```

**💡 Запомните:** `*.* @host:514` = «отправлять все логи на хост». Централизованный сбор логов = легче искать проблемы!""",

    506: """📦 **Cgroups (control groups)** — ограничение ресурсов.

**Что делает:**
- Ограничивает CPU, память, дисковый I/O для групп процессов
- Механизм ядра Linux

**Как использует Docker:**
- Каждый контейнер — отдельная cgroup
- `docker run --memory=512m` — лимит памяти
- `docker run --cpus=1.5` — лимит CPU
- Контейнер не может выйти за лимиты

**Примеры:**
```bash
# Создать cgroup
cgcreate -g memory:/mygroup

# Установить лимит 512 МБ
echo 536870912 > /sys/fs/cgroup/memory/mygroup/memory.limit_in_bytes

# Запустить процесс в cgroup
cgexec -g memory:mygroup ./app
```

**💡 Запомните:** Cgroups = «клетка для процессов». Docker использует cgroups, чтобы контейнеры не съедали все ресурсы.""",

    507: """🐛 **`gdb` + core dump** — анализ упавшей программы.

**Core dump:**
- Образ памяти процесса в момент падения
- Создаётся при segmentation fault и других критических ошибках

**Анализ:**
```bash
gdb /path/to/binary /path/to/core
```

**Команды в gdb:**
```
bt              # backtrace — стек вызовов в момент падения
info registers  # регистры процессора
frame N         # перейти к фрейму N
list            # показать исходный код
print variable  # значение переменной
```

**Включить core dump:**
```bash
ulimit -c unlimited  # Разрешить создание core dump
```

**Где искать:**
- `/var/crash/`
- `.` (текущая директория)
- Настраивается в `/proc/sys/kernel/core_pattern`

**💡 Запомните:** Программа упала? Core dump + gdb = «машина времени» в момент сбоя. `bt` покажет, где произошла ошибка!"""
}

try:
    updated = 0
    for q_id, explanation in explanations.items():
        # Проверяем, существует ли вопрос
        exists = db.execute(
            text("SELECT 1 FROM questions WHERE id = :qid"),
            {"qid": q_id}
        ).first()
        
        if not exists:
            print(f"⚠️  Вопрос ID={q_id} не найден")
            continue
        
        # Обновляем описание
        db.execute(
            text("UPDATE questions SET explanation = :expl WHERE id = :qid"),
            {"expl": explanation, "qid": q_id}
        )
        updated += 1
        print(f"✅ Обновлён вопрос ID={q_id}")
    
    db.commit()
    print(f"\n🎉 Готово! Обновлено {updated} описаний.")
    
except Exception as e:
    db.rollback()
    print(f"❌ Ошибка: {e}")
    sys.exit(1)

finally:
    db.close()
